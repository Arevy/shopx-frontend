import type { RouteDefinition } from '@/routes/types'

const checkoutRoute: RouteDefinition = {
  id: 'checkout',
  path: '/checkout',
  translationKey: 'Page_Checkout.intro.title',
  showInNavigation: false,
}

export default checkoutRoute
