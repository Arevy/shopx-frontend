import type { RouteDefinition } from '@/routes/types'

const impersonateRoute: RouteDefinition = {
  id: 'impersonate',
  path: '/impersonate',
  translationKey: 'routes.impersonate',
  showInNavigation: false,
}

export default impersonateRoute
