import type { RouteDefinition } from '@/routes/types'

const accountRoute: RouteDefinition = {
  id: 'account',
  path: '/account',
  translationKey: 'routes.account',
  showInNavigation: false,
}

export default accountRoute
