import type { NavigationRoute } from '@/routes/types'

const wishlistRoute: NavigationRoute = {
  id: 'wishlist',
  path: '/wishlist',
  translationKey: 'header.nav.wishlist',
  showInNavigation: true,
  order: 30,
}

export default wishlistRoute
